<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.NDAstro" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash background color -->
        <item name="android:windowSplashScreenBackground">@color/white</item>

        <!-- Animated logo -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>

        <!-- Animation duration -->
        <item name="android:windowSplashScreenAnimationDuration">1000</item>

        <!-- Theme applied after splash -->
        <item name="postSplashScreenTheme">@style/Theme.NDAstro.PostSplash</item>
    </style>

    <style name="Theme.NDAstro.PostSplash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
